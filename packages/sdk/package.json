{
  "name": "@optimistic-oracle/sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK for Optimistic Oracle on Solana",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist",
    "prebuild": "npm run clean && mkdir -p dist/idl",
    "build": "tsc && cp -r src/idl/*.json dist/idl/",
    "postbuild": "echo '✅ IDL copied to dist'",
    "watch": "tsc --watch",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "solana",
    "oracle",
    "optimistic",
    "web3",
    "blockchain"
  ],
  "author": "Optimistic Oracle Team",
  "license": "MIT",
  "dependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@solana/web3.js": "^1.87.6",
    "eventemitter3": "^5.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
